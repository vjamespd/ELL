<section class="trust-band ell-section--tight">
  <div class="ell-container">
    <div class="trust-band__grid">
      {% if section.blocks.size > 0 %}
        {% for block in section.blocks %}
          <div class="trust-badge" {{ block.shopify_attributes }}>
            <span class="trust-badge__title">{{ block.settings.title }}</span>
            <span class="trust-badge__caption">{{ block.settings.caption }}</span>
          </div>
        {% endfor %}
      {% else %}
        <div class="trust-badge">
          <span class="trust-badge__title">Review</span>
          <span class="trust-badge__caption">Documentation Ready</span>
        </div>
        <div class="trust-badge">
          <span class="trust-badge__title">Due Diligence</span>
          <span class="trust-badge__caption">Provider Led</span>
        </div>
        <div class="trust-badge">
          <span class="trust-badge__title">Handling</span>
          <span class="trust-badge__caption">Product Specific</span>
        </div>
        <div class="trust-badge">
          <span class="trust-badge__title">Ordering</span>
          <span class="trust-badge__caption">Institutional Review</span>
        </div>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "ELL trust band",
  "max_blocks": 6,
  "blocks": [
    {
      "type": "badge",
      "name": "Badge",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Review"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption",
          "default": "Documentation Ready"
        }
      ]
    }
  ],
  "settings": []
}
{% endschema %}
